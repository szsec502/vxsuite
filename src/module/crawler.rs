pub struct Crawler {
    url String
}

impl Crawler {
    pub fn new(&self, url: &str) -> Crawler {
        Crawler{
            url,
        }
    }

    pub is_url(&self, url: &str) -> bool {
        false
    }
}
